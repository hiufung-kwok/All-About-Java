/**
 * A demo package to demonstrate how the memento pattern can be used on day-to-day development.
 * In this particular package, the main class is responsible to capture the result of scheduled job,
 * and restore it back to certain date in the case of event replay.
 */
package designPattern.memento.scheduledJob;